# Hexo Butterfly 博客项目 Cursor Rules

## 项目概述
这是一个基于 Hexo 7.3.0 的静态博客项目，使用 Butterfly 5.5.2 主题。项目主要使用 Node.js 生态系统，包含 Pug 模板、Stylus 样式和 Markdown 内容。

## 技术栈
- **框架**: Hexo 7.3.0
- **主题**: Butterfly 5.5.2
- **模板引擎**: Pug (Jade)
- **CSS 预处理器**: Stylus
- **Markdown 渲染器**: markdown-it-plus
- **包管理**: npm
- **部署**: Git (hexo-deployer-git)

## 项目结构
```
myblog/
├── _config.yml              # Hexo 主配置文件
├── source/                  # 源文件目录
│   ├── _posts/             # 博客文章（Markdown）
│   ├── categories/         # 分类页面
│   ├── img/                # 图片资源
│   └── css/                # 自定义样式
├── themes/butterfly/        # Butterfly 主题目录
│   ├── _config.yml         # 主题配置文件
│   ├── layout/             # Pug 模板文件
│   ├── source/             # 主题资源文件
│   │   ├── css/            # Stylus 样式文件
│   │   └── js/             # JavaScript 文件
│   └── scripts/            # Hexo 插件脚本
├── public/                  # 生成的静态文件（构建输出）
└── .deploy_git/            # 部署目录
```

## 编码规范

### 文件命名
- Markdown 文章文件：使用小写字母和连字符，如 `hello-world.md`
- Pug 模板文件：使用小写字母和连字符，如 `head.pug`
- Stylus 文件：使用小写字母和下划线，如 `var.styl`
- JavaScript 文件：使用小写字母和连字符，如 `local-search.js`

### 代码风格
- **Pug 模板**: 使用 2 空格缩进，遵循 Pug 语法规范
- **Stylus**: 使用 2 空格缩进，遵循 Stylus 语法规范
- **JavaScript**: 使用单引号，2 空格缩进，遵循 ES6+ 语法
- **YAML 配置**: 使用 2 空格缩进，注意冒号后的空格

### Markdown 文章
- 使用 Front Matter (YAML) 格式
- 必须包含 `title`、`date`、`categories`、`tags` 等字段
- 文章资源文件夹与文章同名（post_asset_folder: true）
- 图片路径使用相对路径或 `/img/` 开头的绝对路径

## 开发工作流

### 常用命令
- `npm run server` - 启动本地开发服务器
- `npm run build` - 生成静态文件
- `npm run clean` - 清理生成的文件
- `npm run deploy` - 部署到 Git 仓库

### 开发流程
1. 在 `source/_posts/` 目录创建或编辑 Markdown 文章
2. 使用 `npm run server` 启动本地服务器预览
3. 使用 `npm run build` 生成静态文件
4. 使用 `npm run deploy` 部署到 GitHub Pages

## 配置文件说明

### Hexo 主配置 (_config.yml)
- `title`: 网站标题 - "举个栗子"
- `language`: 语言设置 - zh-CN
- `timezone`: 时区 - Asia/Shanghai
- `url`: 网站地址 - https://jugelizi.top
- `theme`: 主题名称 - butterfly
- `permalink`: 永久链接格式 - :year/:month/:day/:title/

### 主题配置 (themes/butterfly/_config.yml)
- 导航菜单配置在 `nav` 和 `menu` 部分
- 代码块主题配置在 `code_blocks` 部分
- 图片配置在 `favicon`、`avatar`、`default_top_img` 等部分
- 第三方服务配置（评论、搜索、分析等）在相应部分

## 重要约定

### 路径处理
- 使用 Hexo 的 `url_for()` 辅助函数生成路径
- 图片资源放在 `source/img/` 目录，访问路径为 `/img/`
- 主题资源路径使用 `theme.asset.*` 配置

### 模板继承
- 主要布局文件在 `themes/butterfly/layout/` 目录
- 使用 `include` 和 `partial` 引入子模板
- 使用 `fragment_cache` 进行模板缓存优化

### 插件开发
- Hexo 插件脚本放在 `themes/butterfly/scripts/` 目录
- 使用 `hexo.extend.helper.register()` 注册辅助函数
- 使用 `hexo.extend.filter.register()` 注册过滤器

## 注意事项

1. **不要直接修改主题文件**: 如需自定义，优先通过配置文件或覆盖文件方式
2. **图片优化**: 使用 WebP 格式，注意图片大小和加载性能
3. **SEO 优化**: 确保文章包含适当的 Front Matter 元数据
4. **代码高亮**: 使用 Prism.js 或 highlight.js，在配置文件中设置
5. **部署前检查**: 部署前务必运行 `npm run build` 检查生成结果
6. **Git 忽略**: `public/` 和 `node_modules/` 已在 .gitignore 中

## 常用功能

### 文章分类和标签
- 分类：在 Front Matter 中使用 `categories: [分类名]`
- 标签：在 Front Matter 中使用 `tags: [标签1, 标签2]`
- 支持多级分类和多个标签

### 代码块
- 使用三个反引号包裹代码
- 指定语言类型以获得语法高亮
- 支持代码复制、折叠等功能（主题配置）

### 数学公式
- 支持 MathJax 和 KaTeX
- 使用 `$...$` 表示行内公式
- 使用 `$$...$$` 表示块级公式

### 图片插入
- 使用 Markdown 语法：`![alt text](/img/image.jpg)`
- 或使用 HTML：`<img src="/img/image.jpg" alt="alt text">`
- 支持图片懒加载（主题配置）

## 调试技巧

1. **清除缓存**: 运行 `npm run clean` 清除生成文件
2. **检查配置**: 确保 `_config.yml` 和主题配置格式正确
3. **查看日志**: 运行 `hexo server` 时查看控制台输出
4. **检查生成文件**: 查看 `public/` 目录确认生成结果

## 性能优化

1. **图片懒加载**: 主题已支持，确保配置启用
2. **CDN 加速**: 使用 CDN 加载第三方资源（主题配置）
3. **代码压缩**: 生产环境启用 CSS/JS 压缩
4. **PWA 支持**: 可配置 PWA 提升用户体验

## 响应式设计
- 主题已内置响应式设计
- 确保在不同设备上测试显示效果
- 移动端优先考虑

## 安全注意事项

1. **敏感信息**: 不要在配置文件中提交 API 密钥等敏感信息
2. **依赖更新**: 定期更新 npm 依赖包
3. **内容审核**: 部署前检查文章内容

## 扩展开发

### 自定义样式
- 在 `source/css/` 目录添加自定义 CSS
- 或在主题的 `source/css/` 目录覆盖样式文件

### 自定义脚本
- 在 `source/js/` 目录添加自定义 JavaScript
- 通过主题配置的 `inject` 选项注入脚本

### 自定义页面
- 在 `source/` 目录创建 Markdown 文件
- 设置 Front Matter 的 `layout: page`

## 文档参考
- Hexo 文档: https://hexo.io/docs/
- Butterfly 主题文档: https://butterfly.js.org/
- Pug 文档: https://pugjs.org/
- Stylus 文档: https://stylus-lang.com/
